#include <bits/stdc++.h>
using namespace std;

int main() {
    list<int> list ={10, 20, 30, 40, 50};
    // list.emplace_back(10);
    // list.push_back(20);
    // list.push_front(30);
    // list.push_front(40);

    // list.pop_back();
    // list.pop_front();

    for(int lists : list){
        cout << lists << " ";

    }
    cout << endl; 
    return 0;
}